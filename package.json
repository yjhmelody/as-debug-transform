{
  "name": "debug-transform",
  "version": "0.1.0",
  "description": "A debug decorator for AssemblyScript",
  "license": "MIT",
  "main": "dist/index.js",
  "author": "yjhmelody <yjh465402634@gmail.com>",
  "scripts": {
    "checks": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf dist/* && tsc -p tsconfig.json",
    "test": "jest ."
  },
  "dependencies": {
    "assemblyscript": "^0.19.6",
    "visitor-as": "0.7"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^27.0.6",
    "ts-jest": "^26.5.4",
    "ts-node": "^9",
    "typescript": "^4.3"
  }
}
